---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.js
  - tailwind.config.js
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/(tabs)/bible/page.tsx
  - app/(tabs)/devotionals/page.tsx
  - app/(tabs)/workouts/page.tsx
  - app/(tabs)/prayer/page.tsx
  - app/(tabs)/resources/page.tsx
  - app/(tabs)/connect/page.tsx
  - components/nav/BottomNav.tsx
  - components/nav/TopBar.tsx
  - lib/supabase/client.ts
  - lib/supabase/server.ts
  - .env.local.example
  - README.md
autonomous: false
---

<objective>
## Goal
Bootstrap the Gamebreakers App: Next.js 14 project with GBA branding, Supabase client wired up, Tailwind + shadcn/ui installed, and a mobile nav shell with all 6 feature tabs stubbed and deployed to GitHub.

## Purpose
Establishes the technical foundation every subsequent phase builds on — correct stack choices locked in, project structure clear, and a live repo Kevin and Paul can both access.

## Output
- Next.js 14 App Router project in `/gamebreakers-app/`
- GitHub repo: pmhudak13/gamebreakers-app (public)
- Supabase client configured (env vars via .env.local)
- GBA brand colors applied globally
- Mobile bottom nav with 6 tabs (Home, Bible, Devotionals, Workouts, Prayer, Resources, Connect)
- Each tab has a stub page (title + placeholder content)
- Initial Vercel deploy (manual step at checkpoint)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Key Design Decisions (from STATE.md)
- D-01: Next.js 14 App Router
- D-02: Supabase for backend
- D-03: Tailwind + shadcn/ui
- D-05: Vercel for hosting
- GBA brand: #01003b navy, #a4a4a4 gray, #ffffff white
</context>

<acceptance_criteria>

## AC-1: Project Runs Locally
```gherkin
Given the repo is cloned and `npm install` run
When `npm run dev` is executed
Then the app opens at localhost:3000 with no errors
```

## AC-2: GBA Branding Applied
```gherkin
Given the app is running
When any page is loaded
Then the GBA navy (#01003b) is the primary background/header color
And white text is used for contrast
And the GBA logo or wordmark appears in the top bar
```

## AC-3: Mobile Nav Shell Works
```gherkin
Given the app is running on a mobile viewport (375px)
When the bottom nav is visible
Then 6 tabs are shown: Home, Bible, Devotionals, Workouts, Prayer, Resources
And tapping each tab navigates to its stub page
And the active tab is highlighted in the nav
```

## AC-4: Supabase Client Connected
```gherkin
Given .env.local has NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY set
When the app loads
Then no Supabase connection errors appear in the console
```

## AC-5: GitHub Repo Exists
```gherkin
Given the plan is complete
When github.com/pmhudak13/gamebreakers-app is visited
Then the repository is public and contains the full project
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 project with Tailwind + shadcn/ui</name>
  <files>package.json, next.config.js, tailwind.config.js, app/globals.css, components.json</files>
  <action>
    Run in the repo root:
    ```
    npx create-next-app@latest gamebreakers-app --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"
    cd gamebreakers-app
    npx shadcn@latest init
    ```
    shadcn init options: Default style, Zinc base color (we'll override with GBA colors), CSS variables: yes.

    Then in tailwind.config.js, add GBA brand colors to the theme:
    ```js
    colors: {
      gba: {
        navy: '#01003b',
        gray: '#a4a4a4',
        white: '#ffffff',
      }
    }
    ```

    In app/globals.css, set CSS variables for shadcn to use GBA colors.
    Avoid: using `--foreground` as pure white on pure black — use GBA navy as base.
  </action>
  <verify>npm run dev starts with no errors; tailwind.config.js contains gba color tokens</verify>
  <done>AC-1 satisfied: project runs locally</done>
</task>

<task type="auto">
  <name>Task 2: Apply GBA brand and create mobile layout shell</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css, components/nav/BottomNav.tsx, components/nav/TopBar.tsx</files>
  <action>
    Create `components/nav/TopBar.tsx`:
    - Fixed top bar, GBA navy background (#01003b), white text
    - Shows "Gamebreakers" wordmark (text logo until real logo added)
    - Height: 56px

    Create `components/nav/BottomNav.tsx`:
    - Fixed bottom nav bar, GBA navy background
    - 6 tab items with icons + labels: Home (house), Bible (book), Devotionals (heart), Workouts (dumbbell), Prayer (hands), Resources (grid)
    - Use lucide-react icons (already installed with shadcn)
    - Active tab: white text + icon; inactive: gray (#a4a4a4)
    - Uses Next.js `usePathname()` to detect active route

    Update `app/layout.tsx`:
    - Import TopBar + BottomNav
    - Main content area has padding-top: 56px and padding-bottom: 64px (space for fixed nav bars)
    - Background: white body, content inside the nav bars

    Update `app/page.tsx` (Home):
    - Simple hero: "Welcome to Gamebreakers" in GBA navy, subtitle "Faith. Fitness. Future."
    - 6 feature cards linking to each tab section
  </action>
  <verify>Mobile viewport (375px) shows bottom nav with 6 tabs; clicking each navigates correctly; active tab highlighted</verify>
  <done>AC-2 and AC-3 satisfied: GBA branding visible, mobile nav works</done>
</task>

<task type="auto">
  <name>Task 3: Create stub pages for all 6 feature tabs</name>
  <files>
    app/bible/page.tsx,
    app/devotionals/page.tsx,
    app/workouts/page.tsx,
    app/prayer/page.tsx,
    app/resources/page.tsx,
    app/connect/page.tsx
  </files>
  <action>
    Create a stub page for each feature tab. Each page should:
    - Have a page title in GBA navy (e.g., "Bible", "Devotionals")
    - A brief 1-sentence description of what's coming
    - A placeholder card or empty state graphic (can use a simple colored div)
    - Use the same layout (no duplicate TopBar/BottomNav — those are in layout.tsx)

    Route structure:
    - /bible → Bible reader stub
    - /devotionals → Devotionals stub
    - /workouts → Workout plans stub
    - /prayer → Prayer requests stub
    - /resources → Resources hub stub
    - /connect → Student-Tutor connect stub

    Avoid creating a complex layout here — each page is 20-40 lines max.
  </action>
  <verify>Navigating to each route in the browser shows the correct stub page title</verify>
  <done>AC-3 satisfied: all 6 routes accessible from bottom nav</done>
</task>

<task type="auto">
  <name>Task 4: Wire up Supabase client</name>
  <files>lib/supabase/client.ts, lib/supabase/server.ts, .env.local.example, app/layout.tsx</files>
  <action>
    Install Supabase:
    ```
    npm install @supabase/supabase-js @supabase/ssr
    ```

    Create `lib/supabase/client.ts` (browser client):
    ```ts
    import { createBrowserClient } from '@supabase/ssr'
    export function createClient() {
      return createBrowserClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      )
    }
    ```

    Create `lib/supabase/server.ts` (server client using cookies):
    ```ts
    import { createServerClient } from '@supabase/ssr'
    import { cookies } from 'next/headers'
    export async function createClient() {
      const cookieStore = await cookies()
      return createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        { cookies: { getAll() { return cookieStore.getAll() } } }
      )
    }
    ```

    Create `.env.local.example`:
    ```
    NEXT_PUBLIC_SUPABASE_URL=your-project-url
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
    ```

    Add `.env.local` to .gitignore (should already be there from create-next-app).
    Avoid: initializing the Supabase client at module level outside a function — always inside the factory function.
  </action>
  <verify>npm run build completes without TypeScript errors; no Supabase errors in console when app runs with valid env vars</verify>
  <done>AC-4 satisfied: Supabase client connected</done>
</task>

<task type="auto">
  <name>Task 5: Write README and push to GitHub</name>
  <files>README.md</files>
  <action>
    Create `README.md` with:
    - Project name + GBA description
    - Features list (6 core features)
    - Tech stack table
    - Getting started instructions (clone, install, env setup, dev)
    - Environment variables section (point to .env.local.example)
    - Deployment section (Vercel + Supabase)

    Push everything to GitHub repo pmhudak13/gamebreakers-app (already created via MCP).
  </action>
  <verify>github.com/pmhudak13/gamebreakers-app exists and shows the README</verify>
  <done>AC-5 satisfied: GitHub repo exists with full project</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Next.js app with GBA branding, mobile nav shell, 6 stub pages, Supabase wired up, pushed to GitHub</what-built>
  <how-to-verify>
    1. Run: `cd gamebreakers-app && npm run dev`
    2. Visit: http://localhost:3000 on a mobile-sized browser window (375px)
    3. Confirm: GBA navy header visible with "Gamebreakers" wordmark
    4. Confirm: Bottom nav shows 6 tabs — tap each one to verify navigation
    5. Confirm: Home page shows "Faith. Fitness. Future." tagline
    6. Visit: github.com/pmhudak13/gamebreakers-app — confirm repo is public with code
    7. Optional: Deploy to Vercel by connecting the GitHub repo at vercel.com/new
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Phase 02 (Auth), or describe any issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- This repo's existing files (booklet/, summer-program/, docs/, etc.) — the Gamebreakers App is a new subdirectory `gamebreakers-app/`
- netlify.toml (existing deployment config)

## SCOPE LIMITS
- No authentication in this plan — that's Phase 02
- No database tables yet — Supabase client only, no schema
- No real Bible API calls — stub pages only
- No payment or Stripe integration
- Keep each stub page under 50 lines

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` runs without errors
- [ ] `npm run build` passes TypeScript check
- [ ] All 6 tab routes return 200
- [ ] GBA navy color visible in header and bottom nav
- [ ] .env.local is NOT committed to git
- [ ] github.com/pmhudak13/gamebreakers-app is accessible
</verification>

<success_criteria>
- All 5 tasks completed
- All verification checks pass
- GitHub repo is public and contains the project
- Human verify checkpoint approved
</success_criteria>

<output>
After completion, create `.paul/phases/01-foundation/01-01-SUMMARY.md`
</output>
